/**
 * Public boundary for new free-plan signups.
 *
 * The website and the app's first-party signup route now feed the same
 * consent-gated assessment and plan-persistence flow.
 */
export const NEW_PLAN_INTAKE_OPEN = true;
